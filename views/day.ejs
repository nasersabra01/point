<!DOCTYPE html>
<html lang="ar" dir="rtl">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>
        <%= page %> -
            <%= dayjs(openShift.startTime).format('dddd') %>
                <%= dayjs(openShift.startTime).format('YYYY-MM-DD') %>
    </title>

    <!-- Google Fonts -->
    <link rel="stylesheet" href="/Libraries/fontawesome/css/all.min.css"> <!-- fontawesome css file -->
    <link rel="stylesheet" href="/Libraries/bootstrap/css/bootstrap.rtl.min.css"> <!-- bootstrap css min file -->

    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="/css/main.css">
    <style>
        .modern-tabs {
            display: flex;
            width: fit-content;
            justify-content: space-between;
            margin-bottom: 20px;
            flex-wrap: wrap;
        }

        .modern-tabs button {
            margin: 0 2px;
        }



        .tab-btn {
            border: none;
            background: rgba(20, 30, 60, .75);
            color: #b6c2d9;
            padding: 12px 6px;
            border-radius: 14px;
            transition: .3s;
        }

        .tab-btn.active {
            background: linear-gradient(90deg, #5B5CFF, #7A5FFF);
            color: white;
        }

        .tab-section {
            display: none;
        }

        .tab-section.active {
            display: block;
        }


        .orders-card {
            border-radius: 22px;
            overflow: hidden;
        }

        .orders-table tbody tr {
            transition: .25s;
        }

        .orders-table tbody tr:hover {
            background: rgba(91, 92, 255, .08);
        }

        .orders-table td {
            vertical-align: middle;
        }

        .product-cell {
            display: flex;
            align-items: center;
            gap: 14px;
        }

        .product-info {
            width: 100%;
        }

        .product-name {
            color: white;
            font-weight: 600;
            margin-bottom: 4px;
        }

        .overview-grid {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 20px;
            margin-bottom: 25px;
        }

        .overview-card {
            padding: 25px;
            border-radius: 20px;
            color: white;
        }

        .sales-card {
            background: linear-gradient(135deg, #2564eb00, #1d4fd83f);
        }

        .profit-card {
            background: linear-gradient(135deg, #16a34a00, #15803c40);
        }

        .expense-card {
            background: linear-gradient(135deg, #dc262600, #b91c1c3f);
        }

        .net-card {
            background: linear-gradient(135deg, #7c3aed00, #5a21b642);
        }

        .overview-card span {
            display: block;
            margin-bottom: 12px;
            opacity: .9;
        }

        .overview-card strong {
            font-size: 26px;
        }

        .overview-stats {
            display: grid;
            grid-template-columns: repeat(5, 1fr);
            gap: 15px;
            margin-bottom: 25px;
        }

        .mini-stat {
            background: rgba(20, 30, 60, .65);
            border: 1px solid rgba(255, 255, 255, .08);
            border-radius: 16px;
            padding: 18px;
            text-align: center;
        }

        .mini-stat span {
            display: block;
            color: #94a3b8;
            margin-bottom: 8px;
        }

        .mini-stat strong {
            color: white;
            font-size: 22px;
        }

        .overview-summary {
            background: rgba(20, 30, 60, .65);
            border-radius: 20px;
            padding: 25px;
        }

        .product-supplier {
            color: #8da2c0;
            font-size: 12px;
            margin-bottom: 8px;

        }

        .in-arrow i{
            background-color: #a5a5a5;
            color: white;
            padding: 5px;
            border-radius: 50%;
        }
    </style>

</head>



<body>
    <!-- sidebar -->
    <%- include('../views/components/sidebar.ejs') %>

        <main class="main-content" style="margin-top: 20px">

            <div class="page-actions toolbar">
                <div>

                    <%= dayjs(openShift.startTime).format('dddd') %>
                        <%= dayjs(openShift.startTime).format('YYYY-MM-DD') %>
                </div>


                <div class="d-flex <%= openShift.status==='open' ? 'justify-content-between' : 'justify-content-end' %> "
                    style="width:750px;">
                    <a href="/createInvoice/<%= openShift._id %>" class="action-btn primary">
                        <i class="fa-solid fa-circle-plus"></i>
                        إنشاء فاتورة
                    </a>
                    <a href="/addExpensesPage/<%= openShift._id %>" class="action-btn secondary">
                        <i class="fa-solid fa-circle-plus"></i>
                        إضافة مصروف
                    </a>
                    <a href="/addPaymentPage" class="action-btn secondary">
                        <i class="fa-solid fa-circle-plus"></i>
                        إضافة دفعة
                    </a>
                    <a href="/dayReport/<%= openShift._id %>" class="action-btn secondary">
                        <i class="fa-solid fa-print"></i>
                        طباعة تقرير اليوم
                    </a>
                    <% if (openShift.status==='open' ) { %>


                        <form action="/closeShift/<%= openShift._id %>" method="post" class="d-inline"
                            onsubmit="return confirm('هل أنت متأكد من إغلاق هذا اليوم؟')">
                            <button class="action-btn danger">
                                <i class="fa-solid fa-circle-xmark"></i>
                                إغلاق اليوم
                            </button>
                        </form>

                        <% } %>

                </div>
            </div>

            <div id="formMessages" class="formMessages" style="background-color: green;">
            </div>

            <div class="modern-tabs">
                <button class="tab-btn active" data-filter="invoices">الفواتير <%= openShift.totalInvoices %></button>
                <button class="tab-btn" data-filter="all">الكشوفات <%= openShift.totalProducts %></button>
                <button class="tab-btn" data-filter="open">كشوفات مفتوحة <%= openShift.openProducts -
                        openShift.delayedProducts %></button>
                <button class="tab-btn" data-filter="closed">كشوفات مغلقة <%= openShift.closedProducts %></button>
                <button class="tab-btn" data-filter="delayed">كشوفات اجلة <%= openShift.delayedProducts %></button>
                <button class="tab-btn" data-filter="loseWeightProducts">نقص <%= loseWeightProducts.length %>
                        <button class="tab-btn" data-filter="payments">دفعات اليوم <%= payments.length %>
                        </button>
                        <button class="tab-btn" data-filter="expenses">مصاريف <%= openShift.totalExpenses.toFixed(2) %>
                        </button>
                </button>
                <button class="tab-btn" data-filter="overview">نظرة عامة</button>
            </div>

            <div class="tab-section active" id="invoices-section">
                <div class="orders-card">
                    <table class="orders-table">
                        <thead>
                            <tr>
                                <th><i class="fa-solid fa-arrow-down-wide-short"></i></th>
                                <th>#</th>
                                <th>رقم الفاتورة</th>
                                <th>عدد الأصناف</th>
                                <th>اسم المورد</th>
                                <th>إجمالي الفاتورة</th>
                                <th>إجراءات</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% if (invoices.length> 0 ) { %>
                                <% (invoices).forEach((sale, i)=> { %>
                                    <tr class="invoice-row" style="text-align: center;user-select: none;cursor: pointer;"
                                        data-href="/invoiceDetailes/<%= sale._id %>">
                                        <td class="in-arrow"  style="color:var(--text-muted)">
                                            <i id="in-de-arr" class="fa-solid fa-arrow-down-wide-short"></i>
                                        </td>
                                        <td style="color:var(--text-muted)">
                                            <%= i + 1 %>
                                        </td>
                                        <td  style="color:var(--text-muted)">
                                            <div class="d-flex align-items-center justify-content-center">
                                                <div>
                                                    <div style="font-size:13px;font-weight:600">
                                                        <%= sale.invoiceNo %>
                                                    </div>
                                                </div>
                                            </div>
                                        </td>
                                        <td style="color:var(--text-muted)">
                                            <%= sale.productsCount %>
                                        </td>
                                        <td style="color:var(--text-muted)">
                                            <%= sale.supplier.name %>
                                        </td>
                                        <td style="color:var(--text-muted)">
                                            <%= sale.total.toFixed(2) %>
                                        </td>
                                        <td>
                                            <div class="action-btns">
                                                <a href="/createProductPage/<%= sale._id %>"
                                                    class="btn-icon btn-icon--green" title="إضافة صنف">
                                                    <i class="fa-solid fa-circle-plus"></i>
                                                </a>

                                                <a href="/editInvoice/<%= openShift._id %>/<%= sale._id %>"
                                                    class="btn-icon btn-icon--assent" title="تعديل الفاتورة">
                                                    <i class="fa-solid fa-pen"></i>
                                                </a>

                                                <button class="btn-icon btn-icon--red"
                                                    onclick="deleteInvoice('<%= sale._id %>')" title="حذف الفاتورة">
                                                    <i class="fa-solid fa-trash"></i>
                                                </button>
                                            </div>
                                        </td>

                                    </tr>

                                    <% }) %>

                                        <% } %>

                                            <% if (invoices.length <=0 ) { %>
                                                <tr>
                                                    <td colspan="6" style="color: #8e8c8c;">
                                                        لم يتم إضافة أي فاتورة بعد.
                                                    </td>
                                                </tr>

                                                <% } %>

                        </tbody>
                    </table>
                </div>
            </div>

            <div class="tab-section" id="products-section">
                <div class="orders-card">
                    <table class="orders-table">
                        <thead>
                            <tr>
                                <th>#</th>
                                <th>الصنف</th>
                                <th>الوزن / العدد</th>
                                <th>تكلفة الوحدة</th>
                                <th>رقم الفاتورة</th>
                                <th>ربح</th>
                                <th>حالة الوزن</th>
                                <th>حالة البيع</th>
                                <th>إجراءات</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% if (products?.length> 0 ) { %>
                                <% (products || []).forEach((pr, i)=> { %>
                                    <tr style="text-align: center;" data-sale-status="<%= pr.saleStatus %>"
                                        data-invoice-type="<%= pr.invoice.invoiceType %>"
                                        data-stay-status="<%= pr.stayStatus %>">
                                        <td style="color:var(--blue);font-weight:600">
                                            <%= i + 1 %>
                                        </td>
                                        <td>
                                            <div class="product-cell">
                                                <div class="product-info">
                                                    <div class="product-name">
                                                        <%= pr.productName %>
                                                    </div>

                                                    <div class="product-supplier">
                                                        <%= pr.invoice?.supplier?.name || 'بدون مورد' %>
                                                    </div>
                                                </div>
                                            </div>
                                        </td>
                                        <td style="color:var(--text-muted)">
                                            <%= pr.mainWeight %>
                                        </td>
                                        <td style="color:var(--text-muted)">
                                            <% if (pr.invoice.invoiceType==='شراء' ) { %>
                                                <%= pr.purchaseData.wholesalePrice %> ₪
                                                    <% } %>
                                                        <% if (pr.invoice.invoiceType==='كمسيون' ) { %>
                                                            <%= pr.commissionData.commissionRate %> %
                                                                <% } %>
                                        </td>
                                        <td style="color:var(--text-muted)">
                                            <%= pr.invoice.invoiceNo %>
                                        </td>
                                        <td style="color:var(--text-muted)">
                                            <%= pr.profit.toFixed(2) %> ₪
                                        </td>
                                        <td>
                                            <% if (pr.weightStatus==='متطابق' ) { %>
                                                <span class="badge-status badge-success">
                                                    <%= pr.weightStatus %>
                                                </span>
                                                <% } %>
                                                    <% if (pr.weightStatus==='نقص' ) { %>
                                                        <span class="badge-status badge-cancel">
                                                            <%= pr.weightStatus %>
                                                                <% if (pr.saleStatus==='closed' ) { %>
                                                                    - <%= pr.loseWeight %>
                                                                        <% } %>
                                                        </span>
                                                        <% } %>
                                                            <% if (pr.weightStatus==="لم يبع" ) { %>
                                                                <span class="badge-status badge-tran">
                                                                    <%= pr.weightStatus %>
                                                                </span>
                                                                <% } %>
                                        </td>
                                        <td>
                                            <% if (pr.saleStatus==='open' ) { %>
                                                <% if (pr.stayStatus==='ontherDay' ) { %>
                                                    <span class="badge-status badge-success">
                                                        مفتوح - آجل
                                                    </span>
                                                    <% } %>
                                                        <% if (pr.stayStatus==='today' ) { %>
                                                            <span class="badge-status badge-success">
                                                                مفتوح
                                                            </span>
                                                            <% } %>
                                                                <% } %>
                                                                    <% if (pr.saleStatus==='closed' ) { %>
                                                                        <span class="badge-status badge-cancel">
                                                                            مغلق
                                                                        </span>
                                                                        <% } %>
                                        </td>
                                        <td>
                                            <div class="action-btns">
                                                <a href="/productSaleStatment/<%=  pr._id %>"
                                                    class="btn-icon btn-icon--green" title="إنشاء كشف بيع">
                                                    <i class="fa-solid fa-clipboard-list"></i>
                                                </a>
                                                <a href="/invoiceDetailes/<%=  pr.invoice._id %>"
                                                    class="btn-icon btn-icon--assent" title="عرض داخل الفاتورة">
                                                    <i class="fa-solid fa-arrows-to-eye"></i>
                                                </a>
                                                <a href="/productDetailsPage/<%=  pr._id %>" class="btn-icon"
                                                    title="عرض تفاصيل الصنف">
                                                    <i class="fa-solid fa-eye"></i>
                                                </a>

                                            </div>
                                        </td>
                                    </tr>
                                    <% }) %>
                                        <% } %>


                                            <% if (products.length <=0 ) { %>
                                                <tr style="text-align: center;">
                                                    <td colspan="9" style="color: #8e8c8c;">
                                                        لم يتم إضافة أي أصناف بعد
                                                    </td>
                                                </tr>
                                                <% } %>


                        </tbody>
                    </table>
                </div>
            </div>

            <div class="tab-section" id="payments-section">
                <div class="orders-card">
                    <table class="orders-table">
                        <thead>
                            <tr>
                                <th>#</th>
                                <th>التاريخ</th>
                                <th>المورد</th>
                                <th>المبلغ</th>
                                <th>طريقة الدفع</th>
                                <th>من</th>
                                <th>إلى</th>
                                <th>ملاحظات</th>
                                <th>إجراءات</th>
                            </tr>
                        </thead>

                        <tbody>
                            <% if (payments.length> 0) { %>
                                <% payments.forEach((payment, i)=> { %>
                                    <tr style="text-align: center;" class="payment-row"
                                        data-payment="<%= payment.paymentMethod %>"
                                        data-supplier="<%= payment.supplier?.name || '' %>">

                                        <td>
                                            <%= i + 1 %>
                                        </td>

                                        <td>
                                            <%= dayjs(payment.createdAt).format('YYYY-MM-DD') %>
                                        </td>

                                        <td>
                                            <%= payment.supplier?.name || '-' %>
                                        </td>

                                        <td style="color: var(--green); font-weight:700;">
                                            <%= payment.amount.toFixed(2) %> ₪
                                        </td>

                                        <td>
                                            <% if (payment.paymentMethod==='cash' ) { %>
                                                <span class="badge-status badge-success">كاش</span>
                                                <% } else if(payment.paymentMethod==='bank_transfer' ) { %>
                                                    <span class="badge-status badge-tran">تحويل بنكي</span>
                                                    <% } else if(payment.paymentMethod==='trans' ) { %>
                                                        <span class="badge-status badge-tran">تكاليف نقل</span>
                                                        <% } else if(payment.paymentMethod==='other' ) { %>
                                                            <span class="badge-status badge-tran">أخرى</span>
                                                            <% } else if(payment.paymentMethod==='staf' ) { %>
                                                                <span class="badge-status badge-tran">بضاعة</span>
                                                                <% } %>
                                        </td>


                                        <td>
                                            <%= payment.senderAccountName || '-' %>
                                        </td>
                                        <td>
                                            <%= payment.receiverAccountName || '-' %>
                                        </td>
                                        <td>
                                            <%= payment.notes || '-' %>
                                        </td>

                                        <td>
                                            <% if (payment.day.status==='open' ) { %>
                                                <div class="action-btns">
                                                    <a href="/editPaymentPage/<%= payment._id %>"
                                                        class="btn-icon btn-icon--assent">
                                                        <i class="fa-solid fa-pen"></i>
                                                    </a>
                                                    <form action="/deletePayment/<%= payment._id %>" method="POST"
                                                        class="delete-payment-form">
                                                        <button type="submit" class="btn-icon btn-icon--red">
                                                            <i class="fa-solid fa-trash"></i>
                                                        </button>
                                                    </form>
                                                </div>
                                                <% } else {%>
                                                    ---
                                                    <% } %>
                                        </td>

                                    </tr>
                                    <% }) %>
                                        <% } else { %>
                                            <tr>
                                                <td colspan="9" style="text-align:center;color:#94A3B8;">
                                                    لا توجد مدفوعات حتى الآن
                                                </td>
                                            </tr>
                                            <% } %>
                        </tbody>
                    </table>
                </div>
            </div>

            <div class="tab-section" id="expenses-section">
                <div class="orders-card">
                    <table class="orders-table">
                        <thead>
                            <tr>
                                <th>#</th>
                                <th>نوع المصروف</th>
                                <th>المبلغ</th>
                                <th>ملاحظات</th>
                                <th>إجراءات</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% if (expenses?.length> 0 ) { %>
                                <% (expenses || []).forEach((pr, i)=> { %>
                                    <tr style="text-align: center;">
                                        <td style="color:var(--blue);font-weight:600">
                                            <%= i + 1 %>
                                        </td>
                                        <td>
                                            <div class="d-flex align-items-center justify-content-center">
                                                <div>
                                                    <div style="font-size:13px;font-weight:600">
                                                        <%= pr.expType %>
                                                    </div>
                                                </div>
                                            </div>
                                        </td>
                                        <td style="color:var(--text-muted)">
                                            <%= pr.amount.toFixed(2) %> ₪
                                        </td>
                                        <td style="color:var(--text-muted)">
                                            <%= pr.details %>
                                        </td>
                                        <td>
                                            <div class="action-btns">
                                                <a href="/editExpensesPage/<%= pr._id %>"
                                                    class="btn-icon btn-icon--assent" title="تعديل المصروف">
                                                    <i class="fa-solid fa-pen"></i>
                                                </a>

                                                <form action="/deleteExpenses/<%= pr._id %>" method="post"
                                                    class="d-inline"
                                                    onsubmit="return confirm('هل أنت متأكد من حذف هذا المصروف؟')">
                                                    <button class="btn-icon btn-icon--red">
                                                        <i class="fa-solid fa-trash"></i>
                                                    </button>
                                                </form>
                                            </div>
                                        </td>
                                    </tr>
                                    <% }) %>
                                        <tr>
                                            <th style="text-align: center;">#</th>
                                            <th style="color: rgb(232, 45, 45);">مجموع</th>
                                            <th style="text-align: center;color: rgb(232, 45, 45);">
                                                <%= openShift.totalExpenses.toFixed(2) %> ₪
                                            </th>
                                        </tr>
                                        <% } %>


                                            <% if (expenses.length <=0 ) { %>
                                                <tr style="text-align: center;">
                                                    <td colspan="9" style="color: #8e8c8c;">
                                                        لم يتم صرف اي شي اليوم
                                                    </td>
                                                </tr>
                                                <% } %>


                        </tbody>
                    </table>
                </div>
            </div>

            <div class="tab-section" id="loseWeightProducts-section">
                <div class="orders-card">
                    <table class="orders-table">
                        <thead>
                            <tr>
                                <th>#</th>
                                <th>الصنف</th>
                                <th>المورد</th>
                                <th>الكمية الاصلية</th>
                                <th>الكمية المباعة</th>
                                <th>كمية النقص</th>
                                <th>إجراءات</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% if (loseWeightProducts?.length> 0 ) { %>
                                <% (loseWeightProducts || []).forEach((pr, i)=> { %>
                                    <tr style="text-align: center;">
                                        
                                        <td style="color:var(--blue);font-weight:600">
                                            <%= i + 1 %>
                                        </td>
                                        <td>
                                            <div class="d-flex align-items-center">
                                                <div class="order-avatar"
                                                    style="background:linear-gradient(135deg,#3B82F6,#8B5CF6)">
                                                    <%= pr.productName.charAt(0) %>
                                                </div>
                                                <div>
                                                    <div style="font-size:13px;font-weight:600">
                                                        <%= pr.productName %>
                                                    </div>
                                                </div>
                                            </div>
                                        </td>

                                        <td style="color:var(--text-muted)">
                                            <%= pr.invoice?.supplier?.name || '-' %>
                                        </td>
                                        <td style="font-weight:700;">
                                            <%= pr.mainWeight.toFixed(2) %>
                                                <%= pr.unit %>
                                        </td>
                                        <td style="font-weight:700;">
                                            <%= pr.soldWeight.toFixed(2) %>
                                                <%= pr.unit %>
                                        </td>
                                        <td style="font-weight:700;" class="text-danger">
                                            <%= pr.loseWeight.toFixed(2) %>
                                                <%= pr.unit %>
                                        </td>

                                        <td>
                                            <div class="action-btns">
                                                <a href="/productSaleStatment/<%=  pr._id %>"
                                                    class="btn-icon btn-icon--green" title="متابعة البيع">
                                                    <i class="fa-solid fa-clipboard-list"></i>
                                                </a>
                                                <a href="/invoiceDetailes/<%=  pr.invoice._id %>"
                                                    class="btn-icon btn-icon--assent" title="عرض داخل الفاتورة">
                                                    <i class="fa-solid fa-arrows-to-eye"></i>
                                                </a>
                                                <a href="/productDetailsPage/<%=  pr._id %>" class="btn-icon"
                                                    title="عرض تفاصيل الصنف">
                                                    <i class="fa-solid fa-eye"></i>
                                                </a>

                                            </div>
                                        </td>
                                    </tr>
                                    <% }) %>
                                        <% } %>


                                            <% if (loseWeightProducts.length <=0 ) { %>
                                                <tr style="text-align: center;">
                                                    <td colspan="9" style="color: #8e8c8c;">
                                                        لا يوجد نقص.</td>
                                                </tr>
                                                <% } %>


                        </tbody>
                    </table>
                </div>
            </div>

            <div class="page-actions toolbar">
                <div class="tab-section" id="overview-section" style="width: 100%;">

                    <div class="overview-grid">

                        <div class="overview-card sales-card">
                            <span>إجمالي المبيعات</span>
                            <strong>
                                <%= openShift.totalSales.toFixed(2) %> ₪
                            </strong>
                        </div>

                        <div class="overview-card profit-card">
                            <% if (openShift.totalProfit < 0) { %>
                                <span>إجمالي الخسارة</span>
                                <strong class="text-danger">
                                    <%= openShift.totalProfit.toFixed(2) %> ₪
                                </strong>
                                <% } %>
                                    <% if (openShift.totalProfit>= 0) { %>
                                        <span>إجمالي الربح</span>
                                        <strong>
                                            <%= openShift.totalProfit.toFixed(2) %> ₪
                                        </strong>
                                        <% } %>


                        </div>

                        <div class="overview-card expense-card">
                            <span>إجمالي المصاريف</span>
                            <strong>
                                <%= openShift.totalExpenses.toFixed(2) %> ₪
                            </strong>
                        </div>







                        <div class="overview-card net-card">
                            <% if (openShift.netProfit < 0) { %>
                                <span>صافي الخسارة</span>
                                <strong class="text-danger">
                                    <%= openShift.netProfit.toFixed(2) %> ₪
                                </strong>
                                <% } %>
                                    <% if (openShift.netProfit>= 0) { %>
                                        <span>صافي الربح</span>
                                        <strong>
                                            <%= openShift.netProfit.toFixed(2) %> ₪
                                        </strong>
                                        <% } %>
                        </div>

                    </div>
                    <div class="overview-stats">

                        <div class="mini-stat">
                            <span>فواتير شراء</span>
                            <strong>
                                <%= openShift.purchaseInvoices %>
                            </strong>
                        </div>

                        <div class="mini-stat">
                            <span>فواتير كمسيون</span>
                            <strong>
                                <%= openShift.commissionInvoices %>
                            </strong>
                        </div>




                        <div class="mini-stat">
                            <span>كشوفات مفتوحة</span>
                            <strong>
                                <%= openShift.openProducts %>
                            </strong>
                        </div>

                        <div class="mini-stat">
                            <span>كشوفات مغلقة</span>
                            <strong>
                                <%= openShift.closedProducts %>
                            </strong>
                        </div>

                        <div class="mini-stat">
                            <span>كشوفات آجلة</span>
                            <strong>
                                <%= openShift.delayedProducts %>
                            </strong>
                        </div>

                    </div>
                </div>
            </div>


        </main>




        <script src="/Libraries/bootstrap/js/bootstrap.bundle.min.js"></script> <!-- bootstrap js min.js -->
        <script src="/js/main.js"></script>

        <script>
            function showToast() {
                const t = document.getElementById('formMessages');
                if (!t) return;

                window.scrollTo({ left: 0, top: 0, behavior: 'smooth' });
                t.classList.add('show');

                setTimeout(() => t.classList.remove('show'), 3000);
            }
        </script>

        <% if (msg) { %>
            <script>
                document.addEventListener('DOMContentLoaded', () => {
                    const messagesDiv = document.getElementById('formMessages');
                    messagesDiv.innerHTML = "<%= msg %>";
                    showToast();
                });
            </script>
            <% } %>

                <script>
                    const invoiceRow = Array.from(document.querySelectorAll('.invoice-row'));
                    invoiceRow.forEach(row => {
                        row.addEventListener('dblclick', () => {
                            const href = row.getAttribute('data-href');
                            if (href) {
                                window.location.href = href;
                            }
                        });
                    });
                    document.addEventListener('DOMContentLoaded', () => {
                        const tabs = document.querySelectorAll('.tab-btn');
                        const sections = document.querySelectorAll('.tab-section');
                        const productRows = document.querySelectorAll('#products-section tbody tr[data-sale-status]');

                        function showSection(sectionId) {
                            sections.forEach(s => s.classList.remove('active'));
                            document.getElementById(sectionId).classList.add('active');
                        }

                        function filterProducts(filter) {
                            showSection('products-section');

                            productRows.forEach(row => {
                                const saleStatus = row.dataset.saleStatus;
                                const stayStatus = row.dataset.stayStatus;

                                let show = false;

                                if (filter === 'all') show = true;
                                if (filter === 'open' && saleStatus === 'open' && stayStatus === 'today') show = true;
                                if (filter === 'closed' && saleStatus === 'closed') show = true;
                                if (filter === 'delayed' && stayStatus === 'ontherDay') show = true;

                                row.style.display = show ? '' : 'none';
                            });
                        }

                        tabs.forEach(tab => {
                            tab.addEventListener('click', () => {
                                tabs.forEach(t => t.classList.remove('active'));
                                tab.classList.add('active');

                                const filter = tab.dataset.filter;

                                if (filter === 'all') {
                                    filterProducts('all');
                                }

                                else if (filter === 'open') {
                                    filterProducts('open');
                                }

                                else if (filter === 'closed') {
                                    filterProducts('closed');
                                }

                                else if (filter === 'delayed') {
                                    filterProducts('delayed');
                                }

                                else if (filter === 'invoices') {
                                    showSection('invoices-section');
                                }

                                else if (filter === 'expenses') {
                                    showSection('expenses-section');
                                }

                                else if (filter === 'overview') {
                                    showSection('overview-section');
                                }

                                else if (filter === 'payments') {
                                    showSection('payments-section');
                                }
                                else if (filter === 'loseWeightProducts') {
                                    showSection('loseWeightProducts-section');
                                }
                            });
                        });
                    });



                </script>
                <script>
                    async function deleteInvoice(id, force = false) {
                        try {
                            if (!force) {
                                const firstConfirm = confirm('هل تريد حذف الفاتورة؟');
                                if (!firstConfirm) return;
                            }

                            const url = force
                                ? `/deleteInvoice/${id}?force=true`
                                : `/deleteInvoice/${id}`;

                            const res = await fetch(url, {
                                method: 'POST'
                            });

                            const data = await res.json();

                            if (data.success) {
                                alert(data.msg);
                                location.reload();
                                return;
                            }

                            if (data.needsConfirmation) {
                                const secondConfirm = confirm(data.msg);

                                if (secondConfirm) {
                                    await deleteInvoice(id, true);
                                }

                                return;
                            }

                            alert(data.msg);

                        } catch (error) {
                            console.error(error);
                            alert('حدث خطأ أثناء حذف الفاتورة');
                        }
                    }
                </script>
</body>

</html>