<!DOCTYPE html>
<html lang="ar" dir="rtl">

<head>
    <meta charset="UTF-8" />
    <title>فاتورة كمسيون</title>
    <link rel="stylesheet" href="/Libraries/fontawesome/css/all.min.css"> <!-- fontawesome css file -->
    <link rel="stylesheet" href="/Libraries/bootstrap/css/bootstrap.rtl.min.css"> <!-- bootstrap css min file -->
    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            direction: rtl;
            font-family: Arial, sans-serif;
            background: #f5f5f5;
            padding: 8px;
            font-size: 11px;
            color: #222;
        }

        .invoice {
            width: 100%;
            max-width: 100%;
            margin: auto;
            background: white;
            padding: 15px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, .08);
        }

        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 10px;
            border-bottom: 2px solid #2d2929;
            padding-bottom: 6px;
            margin-bottom: 10px;
        }

        .company h4 {
            color: #2c3e50;
            font-size: 15px;
            margin-bottom: 5px;
        }

        .company p,
        .timeDay {
            color: #777;
            font-size: 10px;
            margin-bottom: 3px;
        }

        .timeDay strong {
            color: #000;
            font-weight: bold;
        }

        .invoice-title {
            text-align: center;
        }

        .invoice-title h2 {
            color: #27ae60;
            font-size: 20px;
            margin-bottom: 5px;
        }

        .invoice-title small {
            color: #2c3e50;
            font-size: 11px;
        }

        .invoice-info {
            display: flex;
            justify-content: space-between;
        }

        .info-box {
            width: 48%;
            padding: 0 5px;
            border-radius: 6px;
            border: 1px solid #404040;
        }

        .info-box p {
            margin: 6px 0;
            color: #444;
            font-size: 11px;
        }

        table {
            width: 100%;
            border-collapse: collapse;

        }

        th {
            background: #220946;
            color: white;
            font-size: 12px;
            text-align: center;
        }

        td {
            border: 1px solid #393737;
            text-align: center;
            font-size: 11px;
            padding: 3px;
        }

        tr:nth-child(even) {
            background: #fafafa;
        }

        .notes-card {
            width: 60%;
            padding: 10px;
            border: 1px solid #d6d6d6;
            border-radius: 6px;
            background: #fcfcfc;
        }

        .notes-card h4 {
            margin-bottom: 6px;
            color: #2c3e50;
            font-size: 12px;
        }

        .notes-card p {
            color: #444;
            font-size: 10px;
            line-height: 1.5;
        }

        .summary {
            width: 35%;
            height: 100px;
            margin-right: auto;
            border: 1px solid #323232;
            padding: 0px 5px;
            background: #fafafa;
            border-radius: 6px;
        }

        .summary-row {
            display: flex;
            justify-content: space-between;
            font-size: 9px;
            border-bottom: 1px solid #ddd;
            padding-bottom: 2px;
        }

        .summary-row.total {
            font-size: 10px;
            font-weight: bold;
            color: #27ae60;
        }

        .alert {
            padding: 8px;
            font-size: 11px;
            color: #444;
            text-align: center;
        }

        .backbtn {
            width: 80px;
            text-align: center;
            text-decoration: none;
            background-color: #1b043b;
            color: white;
            border: none;
            border-radius: 6px;
            position: absolute;
            left: 10px;
            top: 10px;
            padding: 6px;
            font-size: 11px;
            transition: .3s;
        }

        .backbtn:hover {
            opacity: .7;
        }

        @page {
            size: A5 portrait;
        }

        @media print {

            body {
                background: white;
                padding: 0;
            }

            .invoice {
                width: 100%;
                max-width: 100%;
                border-radius: 0;
                box-shadow: none;
            }

            .backbtn {
                display: none;
            }

            th {
                border: 1px solid #242424;
                color: #000;
                font-weight: bold;
            }

            .header,
            .invoice-info,
            .summary-row {
                page-break-inside: avoid;
            }

            table {
                page-break-inside: auto;
            }

            tr {
                page-break-inside: avoid;
            }
        }
    </style>
</head>

<body>
    <a href="/invoiceDetailes/<%= invoice._id %>" class="action-btn primary backbtn">
        <i class="fa-solid fa-arrow-right"></i>
        رجوع
    </a>

    <div class="invoice">

        <div class="header">
            <div class="company">
                <h4>شركة النصر (المتربيعي)</h4>
                <p>لتجارة الخضروات والفواكه</p>
                <p>هاتف: 0597077810</p>
            </div>

            <div class="invoice-title">
                <% if (invoice.invoiceType==='شراء' ) { %>
                    <h2>فاتورة شراء</h2>
                    <% } %>
                        <% if (invoice.invoiceType==='كمسيون' ) { %>
                            <h2>فاتورة كمسيون</h2>
                            <% } %>
                                <small>
                                    <%= invoice.invoiceNo %>
                                </small>
            </div>

            <div class="timeDay">
                <p><strong>تاريخ الطباعة:</strong>
                    <%= dayjs(new Date()).format('D/M/YYYY') %>
                </p>
                <p><strong>وقت الطباعة :</strong>
                    <%= dayjs(new Date()).format('hh:mm:ss') %>
                </p>
            </div>
        </div>

        <div class="d-flex justify-content-between">
            <div style="text-decoration: underline;">
                <p><strong>اسم المورد : </strong>
                    <%= invoice.supplier.name %>
                </p>
            </div>

            <div style="text-decoration: underline;">
                <div>
                    <p><strong>تاريخ الفاتورة:</strong>
                        <%= dayjs(invoice.day.startTime).format('DD/MM/YYYY') %>
                    </p>
                </div>
            </div>



        </div>

        <table>
            <thead>
                <tr>
                    <!-- <th>#</th> -->
                    <% if (invoice.invoiceType==='شراء' ) { %>
                        <th>الصنف</th>
                        <th>الوزن / العدد</th>
                        <th>تكلفة الوحدة</th>
                        <th>مجموع</th>
                        <th>متوسط البيع</th>
                        <% } %>
                </tr>
            </thead>
            <tbody>
                <% if (invoice.invoiceType==='شراء' ) { %>
                    <% if (products?.length> 0 ) { %>
                        <% (products || []).forEach((pr, i)=> { %>
                            <tr style="text-align: center;" style="text-align:center;">
                                <td style="color:var(--blue);font-weight:600">
                                    <%= pr.productName %>
                                </td>

                                <td style="color:var(--text-muted)">
                                    <%= pr.mainWeight.toFixed(2) %>
                                        <%= pr.unit %>
                                </td>

                                <td style="color:var(--text-muted)">
                                    <%= pr.purchaseData.wholesalePrice.toFixed(2) %> ₪
                                </td>


                                <td style="color:var(--text-muted)">
                                    <%= pr.purchaseData.totalCost.toFixed(2) %> ₪
                                </td>
                                <td style="color:var(--text-muted)">
                                    <%= pr.avarageSePrice.toFixed(2) %> ₪

                                </td>
                            </tr>
                            <% }) %>
                                <% } %>
                                    <% } %>
                                        <% if (products.length <=0 ) { %>
                                            <tr style="text-align: center;">
                                                <td colspan="9" style="color: #8e8c8c;">
                                                    لم يتم إضافة أي أصناف للفاتورة بعد.
                                                </td>
                                            </tr>
                                            <% } %>

                                                <% if (invoice.invoiceType==='كمسيون' ) { %>

                                                    <% products.forEach(product=> { %>



                                                        <table class="mt-2 mb-4">
                                                            <thead>
                                                                <tr>
                                                                    <td style="text-align: start; padding: 0 10px;"
                                                                        colspan="2">اسم الصنف: <%= product.productName
                                                                            %>
                                                                    </td>
                                                                    <td style="text-align: end; padding: 0 10px;"
                                                                        colspan="2">الكمية المستلمة: <%=
                                                                            product.mainWeight.toFixed(2) %>
                                                                            <%= product.unit %>
                                                                    </td>
                                                                </tr>
                                                                <tr>
                                                                    <th>الصنف</th>
                                                                    <th>الكمية</th>
                                                                    <th>السعر</th>
                                                                    <th>الإجمالي</th>
                                                                </tr>
                                                            </thead>

                                                            <tbody>
                                                                <% const productSales=saleStatmentt.filter( sale=>
                                                                    sale.product.toString() ===
                                                                    product._id.toString()
                                                                    );
                                                                    %>

                                                                    <% if (productSales.length> 0) { %>
                                                                        <% const groupedSales=productSales.reduce((acc,
                                                                            sale)=> {
                                                                            const key = Number(sale.price).toFixed(2);
                                                                            if (!acc[key]) {
                                                                            acc[key] = { price: Number(sale.price),
                                                                            quantity: 0, total: 0 };
                                                                            }
                                                                            acc[key].quantity += Number(sale.quantity ||
                                                                            0);
                                                                            acc[key].total += Number(sale.total || 0);
                                                                            return acc;
                                                                            }, {});
                                                                            %>
                                                                            <%
                                                                                Object.values(groupedSales).forEach(group=>
                                                                                { %>
                                                                                <tr>
                                                                                    <td>
                                                                                        <%= product.productName %>
                                                                                    </td>
                                                                                    <td>
                                                                                        <%= group.quantity.toFixed(2) %>
                                                                                    </td>
                                                                                    <td>
                                                                                        <%= group.price.toFixed(2) %>
                                                                                            ₪
                                                                                    </td>
                                                                                    <td>
                                                                                        <%= group.total.toFixed(2) %>
                                                                                            ₪
                                                                                    </td>
                                                                                </tr>
                                                                                <% }) %>
                                                                                    <tr>
                                                                                        <td><strong>إجمالي</strong></td>
                                                                                        <td>
                                                                                            <%= product.soldWeight.toFixed(2)
                                                                                                %>
                                                                                        </td>
                                                                                        <td>--</td>

                                                                                        <td>
                                                                                            <%= product.sales.toFixed(2)
                                                                                                %>
                                                                                                ₪
                                                                                        </td>
                                                                                    </tr>
                                                                                    <% } else { %>
                                                                                        <tr>
                                                                                            <td colspan="3">لا توجد
                                                                                                مبيعات لهذا الصنف
                                                                                            </td>
                                                                                        </tr>
                                                                                        <% } %>
                                                            </tbody>
                                                        </table>

                                                        <% }) %>

                                                            <% } %>


            </tbody>
        </table>


        <div class="d-flex">
            <div class="notes-card">
                <h4>الملاحظات</h4>
                <p>
                    <%= invoice.notes %>
                </p>
            </div>

            <div class="summary">
                <% if (invoice.invoiceType==='كمسيون' ) { %>
                    <div class="summary-row">
                        <span>إجمالي المبيعات</span>
                        <span>
                            <%= totalSales.toFixed(2) %> ₪
                        </span>
                    </div>
                    <div class="summary-row">
                        <span> تكلفة نقل</span>
                        <span>
                            <%= invoice.transportCost.toFixed(2) %> ₪
                        </span>
                    </div>
                    <div class="summary-row">
                        <span>كمسيون</span>
                        <span>
                            <%= totalCommission.toFixed(2) %> ₪
                        </span>
                    </div>
                    <div class="summary-row">
                        <span>المسحوبات <%= invoice.taken || '' %></span>
                        <span>
                            <%= Number(invoice.takenVal || 0).toFixed(2) %> ₪
                        </span>
                    </div>

                    <% } %>

                        <div class="summary-row total">
                            <span>إجمالي الفاتورة</span>
                            <span>
                                <%= invoice.total.toFixed(2) %> ₪
                            </span>
                        </div>
            </div>
        </div>
        <hr>

        <% if (invoice.invoiceType==='كمسيون' ) { %>
            <p class="alert">عدم إمكانية المراجعة بعد مرور يومين - 48 ساعة - من إستلام الفاتورة</p>
            <% } %>



    </div>

    <script>
        window.onload = () => window.print();
    </script>

</body>

</html>